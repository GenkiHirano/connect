<div id="favorite-<%= @live_companion.id %>">
  <% if !current_user.nil? && current_user.favorite?(@live_companion) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>