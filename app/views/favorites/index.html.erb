<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h3>　気になる (<%= @favorites.count %>)</h3>
    </div>
  </div>
  <% if @favorites.present? %>
    <ol class="favorites">
      <%= render @favorites %>
    </ol>
  <% end %>
</div>